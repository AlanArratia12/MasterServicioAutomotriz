extends layout

block content
  .container.mt-5.text-center
    h1.text-danger Error 500
    p Lo sentimos, ocurrió un error interno en el servidor.
    a.btn.btn-primary.mt-3(href="/") Regresar al inicio
